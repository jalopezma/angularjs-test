(function() {
    angular
        .module('app.bookmakers')
        .config(config);

    function config($routeProvider) {
        $routeProvider
            //.when('/bookmakers/:bookmaker', {
                //templateUrl: 'bookmakers/bookmaker.html',
                //controller: 'BookmakerController',
                //controllerAs: 'vm'
            //})
            ;
    }
})();
